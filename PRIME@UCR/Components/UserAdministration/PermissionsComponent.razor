@using PRIME_UCR.Components.Controls
@using BlazorTable
@using PRIME_UCR.Domain.Models.UserAdministration;

<Loading IsLoading="ListPermissions == null">

    <EditForm Model="Value">
        <Table TableItem="Permiso" Items="ListPermissions" PageSize="12" >
            <Column TableItem="Permiso" Title="" Field="@(x => x.IDPermiso)"  Width="1%">
                <Template Context="checkbox">
                    <center>
                        <div class="custom-control custom-checkbox" >
                            <input class="custom-control-input" type="checkbox" id="customCheck @checkbox.IDPermiso" checked="@Value.CheckedPermissions[checkbox.IDPermiso-1]" @onclick="((MouseEventArgs b) => update_profile(checkbox.IDPermiso))" />
                            <label class="custom-control-label" for="customCheck @checkbox.IDPermiso"></label>
                        </div>
                    </center>
                </Template>
            </Column>
            <Column TableItem="Permiso" Title="Número de permiso" Field="@(x => x.IDPermiso)"  Width="14%"  />
            <Column TableItem="Permiso" Title="Descripción de permiso" Field="@(x => x.DescripciónPermiso)" Width="35%"/>
       
            <Pager ShowPageNumber="true" />
        </Table>
    </EditForm>
</Loading>
