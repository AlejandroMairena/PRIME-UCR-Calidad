@inject AuthenticationStateProvider _authenticationStateProvider

<p>
    @GetMessageAsync().Result
</p>
@if(@isAuthenticated){
    <button class="btn btn-secondary btn-lg disabled" role="button" aria-disabled="true">Solicitar Permiso</button>
}
