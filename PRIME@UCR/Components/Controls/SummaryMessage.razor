@if (Content.Count == Values.Count && Content.Count > 0)
{
    <div class="@($"alert alert-{Class}") container" role="alert">
        <div class="row">
            @for (var it = 0; it < Content.Count; ++it)
            {
                <div class="col text-center">
                    <b>@Content[it]</b> @Values[it] <br />
                </div>
            }
        </div>
    </div>
}

@code {
    [Parameter]
    public List<string> Values { get; set; }

    [Parameter]
    public List<string> Content { get; set; }

    [Parameter]
    public string Class { get; set; }
}

@*
    Poner esto en el razor: <SummaryMessage Content="Summary.Content"Values="Summary.Values" Class="info" />
    Luego, en el cs:
                public IncidentSummary Summary = new IncidentSummary();
                Dentro del metodo de On Initialized (para que sea en el primer metodo cuando carga la pag) ponen:
                      Summary.LoadValues(Incident); -> Aqui el incident es un objeto IncidentDetailsModel del incidente actual      
*@